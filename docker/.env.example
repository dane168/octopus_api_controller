# Google OAuth Client ID (already configured)
GOOGLE_CLIENT_ID=552874738578-m7fli0h4diuli14mkuurd4pdbj9ufumr.apps.googleusercontent.com

# JWT Secret - generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# Encryption key for storing Tuya credentials - generate with: openssl rand -hex 32
ENCRYPTION_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32

# Frontend URL for CORS (will be set to EC2 public IP after deployment)
FRONTEND_URL=http://YOUR_EC2_PUBLIC_IP

# Log level
LOG_LEVEL=info
